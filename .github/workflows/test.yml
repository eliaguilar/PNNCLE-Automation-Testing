name: PNNCLE Automation Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  schedule:
    # Run daily at 2 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Install Playwright browsers
      run: playwright install chromium
    
    - name: Run form tests
      run: pytest -m form_test --html=test-results/form-report.html --self-contained-html
    
    - name: Run content scanning tests
      run: pytest -m content_test --html=test-results/content-report.html --self-contained-html
    
    - name: Upload test reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-reports
        path: test-results/
    
    - name: Run all tests
      run: pytest --html=test-results/full-report.html --self-contained-html
    
    - name: Upload full test report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: full-test-report
        path: test-results/full-report.html

